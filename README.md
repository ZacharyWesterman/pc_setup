# pc_setup

This repo exists purely to streamline setting up ubuntu with all the applications I like.

To install everything, run `./install.sh`. It will prompt for sudoers password, and will then automatically install all apt and web packages I use often, as well as my repositories.
